# راهنمای تست پروژه با XAMPP

## مرحله 1: راه‌اندازی XAMPP

1. **XAMPP را نصب کنید** (اگر نصب نشده):
   - دانلود از: https://www.apachefriends.org/
   - نصب و اجرای XAMPP Control Panel

2. **سرویس‌های لازم را Start کنید**:
   - ✅ Apache (برای اجرای PHP)
   - ✅ MySQL (برای دیتابیس)

## مرحله 2: کپی کردن فایل‌های پروژه

1. **پوشه پروژه را کپی کنید**:
   ```
   از: C:\Users\S\OneDrive\Desktop\NadLab\menu\v1.1
   به: C:\xampp\htdocs\menu
   ```

   **یا** می‌توانید از Symbolic Link استفاده کنید:
   ```powershell
   # در PowerShell با دسترسی Administrator
   cd C:\xampp\htdocs
   mklink /D menu "C:\Users\S\OneDrive\Desktop\NadLab\menu\v1.1"
   ```

## مرحله 3: ایجاد دیتابیس

1. **phpMyAdmin را باز کنید**:
   - مرورگر: `http://localhost/phpmyadmin`
   - یا از XAMPP Control Panel روی "Admin" کنار MySQL کلیک کنید

2. **دیتابیس جدید ایجاد کنید**:
   - روی تب "Databases" کلیک کنید
   - نام دیتابیس: `nadmenu`
   - Collation: `utf8mb4_unicode_ci`
   - روی "Create" کلیک کنید

## مرحله 4: اجرای اسکریپت SQL

1. **دیتابیس `nadmenu` را انتخاب کنید** (از لیست سمت چپ)

2. **تب "Import" را باز کنید**

3. **فایل `database.sql` را انتخاب کنید**:
   - روی "Choose File" کلیک کنید
   - فایل `database.sql` را از پوشه پروژه انتخاب کنید

4. **Format را روی "SQL" تنظیم کنید**

5. **روی "Go" کلیک کنید**

6. **بررسی کنید** که پیام موفقیت نمایش داده شود و جداول ایجاد شده باشند:
   - tenants
   - tenant_themes
   - tenant_time_rules
   - tenant_moods
   - categories
   - menu_items
   - item_moods
   - item_time_availability

## مرحله 5: بررسی تنظیمات API

فایل `api.php` را بررسی کنید (باید این تنظیمات را داشته باشد):
```php
define('DB_HOST', '127.0.0.1');
define('DB_NAME', 'nadmenu');
define('DB_USER', 'root');
define('DB_PASS', '');
```

✅ این تنظیمات برای XAMPP پیش‌فرض درست است.

## مرحله 6: تست API

1. **API را تست کنید**:
   - مرورگر: `http://localhost/menu/api.php?action=menu&tenant_id=cafe_1`
   - باید JSON با ساختار منو برگردانده شود

2. **اگر خطا دیدید**:
   - Console مرورگر را چک کنید (F12)
   - فایل `error_log` در پوشه پروژه را بررسی کنید
   - در phpMyAdmin بررسی کنید که جداول و داده‌ها درست ایجاد شده باشند

## مرحله 7: تست سایت

1. **سایت را باز کنید**:
   - مرورگر: `http://localhost/menu/`
   - یا: `http://localhost/menu/index.html`

2. **بررسی کنید**:
   - ✅ صفحه لودینگ نمایش داده شود
   - ✅ کارت‌های دسته‌بندی نمایش داده شوند
   - ✅ با کلیک روی کارت، منوی آیتم‌ها باز شود
   - ✅ فیلتر مودها کار کند

## مرحله 8: دیباگ (در صورت مشکل)

### مشکل: "Database connection failed"
- ✅ MySQL در XAMPP اجرا شده است؟
- ✅ نام دیتابیس در `api.php` درست است؟
- ✅ دیتابیس `nadmenu` ایجاد شده است؟

### مشکل: "Tenant not found"
- ✅ داده‌های نمونه در دیتابیس وارد شده‌اند؟
- ✅ در phpMyAdmin جدول `tenants` را چک کنید و ببینید ردیف `cafe_1` وجود دارد

### مشکل: "CORS Error" یا "Failed to fetch"
- ✅ Apache در حال اجرا است؟
- ✅ URL درست است؟ (`http://localhost/menu/...`)
- ✅ فایل `api.php` در مسیر درست است؟

### مشکل: "GSAP is not defined"
- ✅ اینترنت متصل است؟ (GSAP از CDN لود می‌شود)
- ✅ یا می‌توانید GSAP را به صورت محلی دانلود کنید

## نکات مهم

1. **پورت‌ها**:
   - Apache معمولاً روی پورت `80` اجرا می‌شود
   - MySQL روی پورت `3306`
   - اگر پورت‌ها اشغال باشند، در XAMPP Config می‌توانید تغییر دهید

2. **مسیر فایل‌ها**:
   - همه مسیرها باید نسبی باشند (مثل `./assets/css/main.css`)
   - اگر از Symbolic Link استفاده می‌کنید، مسیرها درست کار می‌کنند

3. **Tailwind CSS**:
   - فایل `assets/css/main.css` باید از `input.css` ساخته شده باشد
   - اگر تغییر دادید، دوباره بیلد کنید:
     ```bash
     npm run build
     ```

4. **تست در مرورگرهای مختلف**:
   - Chrome/Edge: `http://localhost/menu/`
   - Firefox: `http://localhost/menu/`
   - برای تست موبایل: از Developer Tools استفاده کنید یا از IP لوکال استفاده کنید

## دستورات مفید

```bash
# اگر از Git Bash یا Terminal استفاده می‌کنید:
cd C:/xampp/htdocs/menu

# بیلد Tailwind (اگر تغییر دادید):
npm run build

# یا watch mode برای توسعه:
npm run dev
```

## ساختار نهایی

```
C:\xampp\htdocs\menu\
├── index.html
├── api.php
├── database.sql
├── assets/
│   ├── css/
│   │   └── main.css
│   ├── js/
│   │   ├── main.js
│   │   ├── api.js
│   │   └── ...
│   └── imgs/
└── ...
```

## تست موفقیت‌آمیز

اگر همه چیز درست کار کند:
- ✅ API داده برمی‌گرداند
- ✅ سایت کارت‌های دسته‌بندی را نمایش می‌دهد
- ✅ کلیک روی کارت، منو را باز می‌کند
- ✅ فیلتر مودها کار می‌کند
- ✅ آیتم‌ها بر اساس زمان فیلتر می‌شوند

**موفق باشید! 🎉**
